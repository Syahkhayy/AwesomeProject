module.exports = {
  arrowParens: 'always',          // Always wrap arrow fn params in ()
  bracketSpacing: true,           // { foo: bar }
  bracketSameLine: false,         // Keep > of JSX on its own line
  singleQuote: true,              // Use single quotes for strings
  jsxSingleQuote: true,           // Use single quotes in JSX
  printWidth: 120,                 // Wrap lines at 80 chars
  proseWrap: 'always',            // Wrap markdown text
  quoteProps: 'as-needed',        // Only quote props when needed
  semi: true,                     // Add semicolons
  tabWidth: 2,                    // 2 spaces for indentation
  trailingComma: 'all',           // Commas everywhere possible
  useTabs: false,                 // Use spaces, not tabs
  htmlWhitespaceSensitivity: 'css', // Respect CSS display for HTML
};